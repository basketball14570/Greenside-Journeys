import WebViewScreen from "@/components/WebViewScreen";

// Course guide — hidden route, opened from the You tab's Betting tools.
export default function Guide() {
  return <WebViewScreen path="/dashboard/guide" />;
}
